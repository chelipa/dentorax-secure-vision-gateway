{
  "name": "dentorax-secure-vision-gateway",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "description": "Dentorax Secure Vision Gateway with JSON repair, schema guard, and Prompt Protocol 8.8.3H v0.2.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js",
    "prestart": "npm install --omit=dev --no-audit --no-fund"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
